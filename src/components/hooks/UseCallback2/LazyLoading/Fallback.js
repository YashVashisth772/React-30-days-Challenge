import React from 'react';

export default class Fallback extends React.Component{
    render(){
        return(
            <div>
                <h2>Loading...</h2>
            </div>
        )
    }
}